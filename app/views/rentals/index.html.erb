<div>
  <h2>List rentals</h2>
  <% if current_user %>
    <%= link_to 'New rental', current_user.admin? ? new_admin_rental_path: new_rental_path %>
  <% end %>
</div>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Precio</th>
      <th>Type</th>
      <th>Rentable</th>
      <% if current_user %>
        <th>Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @rentals.each do |rental|%>
        <tr>
          <td><%= rental.id %></td>
          <td><%= rental.paid_price %></td>
          <td><%= rental.rentable_type %></td>
          <td><%= rental.rentable.title %></td>
          <% if current_user %>
            <td>
              <%= link_to 'Edit', edit_admin_rental_path(rental) %>
              <%= link_to 'Destroy', admin_rental_path(rental), method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
          <% end %>
        </tr>
      <% end %>
  </tbody>
</table>
